============
Installation
============

At the command line::

    $ easy_install django-panpub

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-panpub
    $ pip install django-panpub
